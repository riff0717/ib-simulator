# Factory Output Simulator (frontend)

A small React + Vite app that computes civilian factory availability and base construction power.

Commands:
- npm install
- npm run dev
- npm run build

Build output goes to dist/ by default.